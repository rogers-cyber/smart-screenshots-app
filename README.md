# Smart Screenshots App

CLI tool to generate Microsoft Store–ready screenshots for PWAs.

## Usage
1. Edit `config.js`
2. Run `npm install`
3. Run `npm run capture`
4. Upload images to Microsoft Partner Center
